import { GoalsGridOrderPipe } from './goals-grid-order.pipe';

describe('GoalsGridOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new GoalsGridOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
